build:
    box: theotow/gitmonitor-client2
    steps:
      - script:
        name: echo nodejs information
        code: |
          git checkout -- .
      - npm-install:
        options: '-g'
      - npm-test
