build:
    box: theotow/gitmonitor-client2
    steps:
      - internal/shell:
        cmd: git checkout -- .
      - internal/shell:
        cmd: npm i -g
      - internal/shell:
        cmd: npm test
